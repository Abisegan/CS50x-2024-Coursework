# include <cs50.h>
# include <stdio.h>

int main(int argc, string argv[])
{
    // get key
    string key[] = argv[1]
    // validate key
    if (strlen(key) == 25)
    {
        for (int i = 0, len = argc; i < len; i++)
        {

        }
    }
    else
    {
        printf("key must contain 26 characters");
        return 1;
    }
    // get plain text
    // enciper
    // print cipher text
}
