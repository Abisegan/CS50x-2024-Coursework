# include <cs50.h>
# include <stdio.h>

int main(int argc, string argv[])
{
    // get key
    // validate key
    if (argc == 25)
    {
        for (int i = 0, len = strlen())
    }
    else
    {
        printf("key must contain 26 characters");
        return 1;
    }
    // get plain text
    // enciper
    // print cipher text
}
